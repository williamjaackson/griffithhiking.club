# The Sanity Studio is its own package so React and the studio bundle never
# enter the Astro build. One lockfile, one install, one format check.
packages:
  - studio

# esbuild's postinstall links its platform binary, which Astro's build needs.
# Declared here so CI installs match local without an interactive approval.
allowBuilds:
  esbuild: true
